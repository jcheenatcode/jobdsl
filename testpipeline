@Library('mylibrary') _

import com.jcheenatcode.GlobalVars

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                echo 'Hello, world'
                sayHello 'Dave'

                echo 'The value of foo is : ' + GlobalVars.foo
            }
        }
    }
}